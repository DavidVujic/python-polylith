version: '2.1'
orbs:
  python: circleci/python@2.1.1

jobs:
  lint:
    executor:
      name: python/default
      tag: "3.8"
    steps:
      - checkout
      - python/install-packages:
          pkg-manager: poetry
      - run:
          command: |
            poetry run flake8
          name: Linting

workflows:
  main:
    jobs:
      - lint
